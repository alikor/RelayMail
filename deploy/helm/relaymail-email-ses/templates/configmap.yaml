apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "relaymail-email-ses.fullname" . }}-config
  labels:
    {{- include "relaymail-email-ses.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
