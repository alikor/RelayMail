apiVersion: v1
kind: Service
metadata:
  name: {{ include "relaymail-email-ses.fullname" . }}
  labels:
    {{- include "relaymail-email-ses.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "relaymail-email-ses.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
