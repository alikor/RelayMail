apiVersion: v1
kind: Service
metadata:
  name: relaymail-email-ses
  namespace: relaymail
  labels:
    app.kubernetes.io/name: relaymail-email-ses
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relaymail-email-ses
  ports:
    - name: http
      port: 8080
      targetPort: http
