apiVersion: v1
kind: ServiceAccount
metadata:
  name: relaymail-email-ses
  namespace: relaymail
  annotations:
    # EKS IRSA — replace with the IAM role ARN that grants S3/SQS/SES/DDB
    # least-privilege permissions (see deploy/terraform example).
    eks.amazonaws.com/role-arn: "REPLACE_ME"
