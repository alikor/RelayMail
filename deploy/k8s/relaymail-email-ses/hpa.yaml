apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: relaymail-email-ses
  namespace: relaymail
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: relaymail-email-ses
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
