# Future scope — relaymail-events

- Subscribe to SES notifications (SNS/SQS) for bounces, complaints,
  deliveries, clicks, opens.
- Normalize into RelayMail event records; persist for audit.
- Feed suppression lists used by future senders (direct MTA, additional providers, etc).
- Emit metrics per tenant and per provider.

No code in this directory yet.
