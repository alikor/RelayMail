# Future scope — relaymail-console

- Web UI (framework TBD) for operator tasks.
- Reads from `relaymail-events` pipeline + DynamoDB idempotency table.
- Role-based access control.
- Audit log of operator actions.

No code in this directory yet.
